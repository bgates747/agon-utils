# W04 — First slice and sequencing

Maintained decision: the final section of [test strategy](../../../test-strategy.md).

Selected 12 deterministic cases across getError, getleafname and pmatch. Two
valid seed patterns yield 24 observations per route, not an inflated test count.
Preservation claims differ: leafname's unpromised clobbers are characterization.

Existing MAIN-02 comes first; proposed runner foundation, TEST-* controls and
first-slice implementation follow. Proposals are not active tasks or implementation
authorization. Hardware parity and wider coverage have explicit dependencies.
No emulator runs, implementation or firmware changes were performed for W04.
