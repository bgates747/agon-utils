# 2026-09-15 — MOS tests bootstrap

The subproject scaffold and isolated emulator were created on Linux. SETUP-01
records the next AgonDev configuration task; implementation has not started.
Emulator behavior still needs human validation before committing.

## Decisions and evidence

1. Used `examples/mos-tests` consistently; interpreted `examples/most-tests`
   in the request as a spelling variation.
2. Read canonical AGENTS, environment, repositories, documentation and emulator
   guides, plus agon-utils AGENTS and workflow notebook. Consulted
   agon-extender's task README and ADMIN-001 detail record for the task structure.
3. Reused agon-utils `.venv` (Python 3.14.6); ran
   `.venv/bin/python tests/test_agonutils.py` at repository root successfully.
4. Selected installed official Fab 1.2.4 rather than the older 1.2.3 reference.
   Canonical setup verified MOS v3.0.2 Arthur image/map hashes and generated
   `.emulator/` with stock VDP, shared utilities, and owned SD state.
5. Created AGENTS, README, HANDOFF, the sole TODO, documentation indexes, and
   SETUP-01. C++, BBC BASIC, and independent hand-assembly comparisons are
   recorded as the intended testing approaches.
6. Existing repository modifications and deletions outside this subproject
   were present before work and were left untouched. Nothing committed or pushed.
7. No MOS tests, AgonDev build, firmware flashing, or hardware validation ran.
8. Profile checks passed: both MOS checksums, executable non-symlink wrapper,
   raw-runtime symlink, and profile-local `./fab-agon-emulator --help` (runtime
   loads successfully). Git confirms the profile is ignored. These checks do
   not establish graphical boot, MOS execution, audio, or hardware behavior.

## Task convention refinement

1. Adopted the Author's permanent W01..Wnn subtask identifiers, checkbox syntax,
   and explicit Completed/Deferred/Canceled dispositions. Subtasks are retained
   forever after creation; wording can change in place without changing IDs.
2. Converted SETUP-01 scope items into Work subtasks W01 through W07, preserving
   all seven descriptions and leaving every item unfinished.
3. Clarified task-local experiment/trial silos and promotion of mature outputs
   into maintained subproject locations. TODO remains the task index; task
   records own subtask status without duplicating it in TODO.

## Task-local categories clarification

1. Clarified that Work/W is an optional default. A master task may define
   multiple categories with distinct prefixes and IDs unique within that task.
2. No global subtask namespace or registry is maintained. Existing SETUP-01
   W01–W07 identifiers remain unchanged; permanent-ID and disposition rules
   apply equally to every local category.

## TODO authority boundary

1. The Author clarified that the authoritative MOS tests TODO belongs at
   `examples/mos-tests/TODO.md`, not the agon-utils repository root.
2. Made this scope explicit in AGENTS.md, TODO.md, and the task convention;
   canonical project-TODO guidance resolves to the subproject for this work.

## SETUP-01 review refresh

1. Made subproject TODO authority and task-local category/disposition rules
   explicit in SETUP-01. Preserved W01–W07 and their unfinished status.
2. Refreshed the requested local `/tmp/SETUP-01.md` review snapshot; Linux
   remains the authoritative working copy.

## SETUP-01 W01 — Completed

1. Inspected the installed AgonDev 1.0 / Clang 15.0.7 / Binutils 2.45 toolchain,
   official local docs, build configuration, crt0 and representative MOS wrappers.
2. Retained identity, checksums, compiler predefined macros, source excerpts,
   and findings under `docs/tasks/SETUP-01/W01/`. Marked W01 Completed with evidence.
3. Identified missing noninteractive PATH resolution and clarified basic versus
   extended argument parsing. Recorded C++ type widths and unqualified runtime
   features without asserting compiler or MOS correctness.
4. W02–W07 remain unfinished. No upstream edits, test builds, emulator changes,
   hardware operations, commits, or pushes occurred in W01.

## SETUP-01 W02 — Completed

1. Added standard src/include/lib directories, a thin AgonDev Makefile,
   generated-output ignores, and `docs/build.md`. Explicit installation
   resolution handles the noninteractive PATH finding from W01; Python remains
   the existing repository-root environment. C++14 is explicit.
2. Validated the expected empty-source failure, clean dry run, C++/assembly/link
   rule expansion with an explicit installation override, and Git output ignores.
   Commands and exit statuses are in `docs/tasks/SETUP-01/W02/validation.txt`.
   Task-local rule-expansion fixtures were not compiled or executed.
3. Marked W02 Completed, preserving its ID and recording evidence. W03–W07
   remain unfinished; no emulator configuration, deployment, hardware testing,
   upstream changes, commits, or pushes occurred.

## SETUP-01 W03 — Built; human runtime confirmation pending

1. Added `src/main.cpp`: print a fixed marker through MOS putch, then return zero.
   Clean build passed; 1,235-byte binary has the expected MOS version-0 ADL header.
2. Preserved build, binary/map, object disassembly and SHA256 evidence in W03.
   Copied only the binary into the profile-owned mystuff directory, verified its
   hash, preserved previous autoexec and prepared CRLF load/run startup.
3. Launched the canonical profile-local wrapper on Linux Wayland; process was
   alive after three seconds. This does not prove visible output or prompt return.
4. W03 remains unchecked pending human confirmation. No hardware testing,
   commits, or pushes occurred. W04–W07 have not been completed by this work.

## W03 screenshot attempt

1. Captured the Linux desktop through the COSMIC screenshot backend and saved
   `docs/tasks/SETUP-01/W03/emulator-screen.png`. The image shows the locked
   desktop, so it provides no evidence of smoke output or MOS prompt return.
2. Left the launched emulator available for review. W03 remains unfinished;
   unlocking the desktop is needed for visual verification.

## SD-card backend decision

1. The Author relayed emulator maintainer Tom Morton's recommendation to use
   `--sdcard-img` to avoid building tests around hostfs bad behavior.
2. Adopted raw SD image as the intended MOS test baseline in AGENTS.md and
   clarified W06 scope in place. Current W03 hostfs execution is bootstrap
   evidence only; raw-image execution remains pending W06.

## W03 — Completed with headless evidence

1. At the Author's request, terminated the graphical process and executed the
   unchanged binary headlessly through the canonical launcher using SDL dummy
   drivers, software rendering, and the built-in debugger.
2. Verified exact MOS character-output bytes and main returning HL=0. Preserved
   debugger trace, decoded bytes and method in W03; marked W03 Completed.
3. Closed the headless process. No visible-prompt, framebuffer, physical hardware,
   or filesystem-correctness claim follows. Raw-image qualification remains W06;
   nothing committed or pushed.

## Headless execution preference

1. The Author made headless execution the preferred default for testing and
   development. Graphical launches are reserved for absolutely necessary
   human visual confirmation, superseding graphical attention-alert guidance.
2. Recorded this preference in AGENTS.md and HANDOFF.md. No emulator was
   launched or configuration changed for this documentation update.

## SETUP-01 W08 — Completed: debugger reference

1. Added W08 for the requested debugger study, then inspected Fab 1.2.4 README,
   CLI arguments, debugger parser/client/core, and machine SD/backend handling.
2. Created `docs/emulator-debugging.md` covering commands, address triggers,
   memory/disassembly, stepping/tracing, debug I/O, fault stops, headless PTY
   automation, raw-image fidelity, integration patterns and evidence limits.
3. Recorded source/submodule identities under W08, linked the reference from
   AGENTS and docs index, and marked W08 Completed. No new emulator launch or
   test-suite implementation was needed for this read-and-document task.

## SETUP-01 W04 — Completed

1. Created two task-local standard AgonDev projects: C++/libagon putch and
   independent hand-assembly RST 10h output, each with a 24-bit return sentinel.
2. Added a prompt-synchronized PTY runner; clean builds and both headless runs
   passed exact expected MOS output, HL=0x123456, stack balance and IX preservation.
   Retained compiler assembly, disassembly, binary/maps, transcripts and JSON results.
3. Marked W04 Completed. Existing W03 source/binary remained unchanged; temporary
   hostfs deployment and autoexec changes were cleaned/restored. Both owned
   emulator processes exited zero. No commits, pushes, or hardware execution.
4. These are hostfs-bootstrap observations of the output/return boundary only.
   W06 owns raw-image fidelity. A first environment-test command used the SSH
   home directory and failed to locate its relative test path; the corrected
   repository-root agon-utils verification passed before implementation.

## W06 resequenced and executed — qualification incomplete

1. Author approved W06 before W05; recorded W06 → W05 → W07 in TODO and master
   task, moved existing W06 ahead of W05 without changing identifiers.
2. Implemented bounded MBR/FAT32 image generation, two-stage readback and hashes,
   image-required headless launcher, and Make targets. mtools is extracted only
   into ignored profile state; no upstream/system installation changes.
3. Raw-image boot repeatedly faults at PC 0xe58c reading 0xa26300 before main.
   Preserved unpartitioned, zero-RAM, MBR, tracing, precise-interrupt and final
   BPB/readback trials. Trace trial timed out and was terminated cleanly.
4. W06 remains unchecked pending resolution and successful image execution.
   The fault's owner is unresolved. No fallback acceptance, hardware changes,
   commits or pushes; all owned emulator processes are closed.

## W06 macOS comparison

1. Author explicitly authorized a temporary local /tmp trial. Used installed
   macOS x86_64 Fab tag 1.2.4, copied Linux MOS 3.0.2 image/map and the final
   failing SD image (SHA256 8dbbd0c4644f73eb17b39b805f989219d5d378cf18f4ed564d27672ae27ddb46).
2. A /tmp copy of the macOS profile wrapper selected this image; existing Mac
   profiles were unchanged. Headless dummy SDL execution reproduced precisely
   the Linux pre-main fault: access 0xa26300 at PC 0xe58c following 0xe589.
3. Debugger exit was requested; the macOS process terminated with signal 6
   (return code -6), not normal exit. No process remains from this trial.
4. Copied transcripts and trial driver back into W06/trial-07-macos on Linux.
   This rules out a Linux-only symptom, but does not isolate MOS versus shared
   emulator/image behavior. W06 remains incomplete.

## W06 detailed trace for Tom

1. Captured OBEY entry, pre-close stack, fault registers, frame links and
   disassembly. Unwound main → mos_cmdOBEY → f_close → f_sync → validate.
2. Missing !boot.obey returns FR_NO_FILE and bypasses f_open, but OBEY cleanup
   closes an uninitialized stack FIL; its obj.fs is 0xA26300. This source-correlated
   diagnosis narrows the earlier unresolved failure.
3. A copied image with populated !boot.obey passed the C++ headless output,
   24-bit return, stack and IX checks. Empty !boot.obey alone selected an empty
   startup and timed out waiting for the test, as expected from main's fallback logic.
4. Prepared report and compressed failing-image/trace bundle under W06/tom-trace.
   Nothing sent externally. W06 integration remains pending; no MOS fix applied.

## W06 — Completed with preferred !boot.obey startup

1. Author approved testing !boot.obey and proceeding if it resolves the failure.
   Updated image creation to deploy the CRLF startup under /!boot.obey.
2. Both Linux raw-image comparisons passed expected output, 24-bit return,
   stack balance and IX preservation; processes exited zero. Evidence lives
   in W06/evidence. Rebuilt the default smoke image, preserving its predecessor.
3. Marked W06 Completed; next sequence is W05 → W07. Created MOS-01 to
   separately fix missing-OBEY cleanup and prepare an upstream PR. No upstream
   patch, commit or push performed; hardware remains untested.

## W05 — Jeroen's BASIC selected

1. Author selected envenomator/Jeroen Venema BBC BASIC V ADL as first candidate.
   Located the existing clean bbc-basic-v-adl checkout, origin agon-bbc-basic,
   at v1.0RC1. Recorded generated binary identity without assuming release provenance.
2. Identified Tom's curated repo as popup-mos; its installed bundled BASICs
   are other ports. Read the selected port's README and platform details.
3. No clone, upstream modification, interpreter deployment or execution yet.

## W05 interpreter filename

1. Author selected `bbc-basic-v-adl.bin` as the unambiguous deployment name
   for Jeroen's interpreter. Recorded it in project guidance and W05 selection.
2. Upstream `bin/basic.bin` remains the source artifact; no upstream files changed.

## W05 — Completed

1. Exported the pinned Jeroen BASIC v1.0RC1 source into the task silo and rebuilt
   it with installed AgonDev. Binary exactly matches the existing 25,499-byte
   build. Preserved renamed binary/map and build log without editing upstream.
2. Prepared CRLF text BASIC arithmetic/output/QUIT fixture and !boot.obey.
   First invocation lacked RUN's default-address dot and timed out; retained
   failed trial, corrected to RUN . smoke.bas per MOS source.
3. Corrected run and fresh-image helper reproduction both passed exact MOS
   output and RET to MOS PC 0x000D5F with HL=0. Processes exited zero.
4. Saved image builder/observer, manifests and transcripts; marked W05 Completed.
   W07 remains. No graphics, hardware, upstream modifications, commits or pushes.

## W07 — Completed

1. Consolidated three raw-image success receipts, expected/observed output,
   executable/image identity and evidence hashes into W07/run-register.json.
2. Verified retained executable hashes and captured bytes against receipts;
   no redundant emulator runs. Recorded current runtime/VDP hashes explicitly
   as retrospective and documented missing timing/version observations.
3. Added docs/test-results.md. Marked W07 Completed; master task stays in TODO
   pending Author acceptance/commit approval. Hardware untested; no commits/pushes.

## SETUP-01 — Accepted and closed

1. Author explicitly accepted the documented results and approved committing.
   Closed SETUP-01 and removed it from the authoritative unfinished TODO; all
   immutable subtasks and trial evidence remain retained. MOS-01 stays open.
2. Consolidated HANDOFF and corrected stale README/task status. Acceptance covers
   documented headless evidence; no new visual or hardware result is claimed.
3. Commit scope is examples/mos-tests only; unrelated repository edits are preserved.
   No push requested. Runtime checks were already qualified; closeout changes are
   documentation only.
4. Closeout validation: repository Python extension check passed; staged scope
   contains only mos-tests and excludes emulator profiles, source exports and
   build directories. Maintained-file whitespace check passed. Historical
   transcript/compiler-output whitespace is preserved byte-for-byte.
