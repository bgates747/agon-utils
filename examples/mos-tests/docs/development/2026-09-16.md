# Development log — 2026-09-16

## MAIN-03 W05 / TEST-01 W03 and W05 — Completed

The shared host decoder/report passed 58 scenarios, including all 14 frozen
outcome oracles, and rejected 690 truncated plus 690 corrupted records. Summary
counts, known failures on incomplete runs, source identity and recovery limitations
are explicit. Next is W06 editable startup/bundle integration.

1. Added strict manifests/artifact/script validation and bounded typed v1 decoding,
   sequence/case/sample/chunk reconciliation, preservation-mask checks, duplicate
   handling and independent SRAM emergency interpretation. Forensic mode cannot
   establish completion; close errors override complete-file claims.
2. Added human report/report-check commands. Summaries lead; unique failing tests
   are counted/listed before details. ANSI white-on-green/red badges reset cleanly;
   saved text is plain. JSON retains decoded evidence from all valid records.
3. Independent fixtures and all CLI checks passed in check-03; evidence copied to
   MAIN-03/W05. Earlier check-01/check-02 runs passed and remain as trials.
4. Updated maintained guides, task status and handoff. No emulator/hardware launch,
   firmware modification, commit or publication. Target assertion claims are not
   independently re-executed by the decoder. W04 placeholder identities cannot be
   presented as authenticated full-suite execution. W06 owns actual integration.

Final wording check: corrected singular failure headline to “1 test failed”;
check-04 passed the same complete set. Retained final evidence separately.

## Checkpoint cycle and backlog authorization

The Author required committed work contracts before implementation, followed by
accepted-result checkpoints that also freeze the next work item. Updated project
rules and task conventions. The Author explicitly authorized committing the
existing backlog in logical groups as if that cycle had been followed. MAIN-02
is accepted for this checkpoint series and removed from the unfinished index;
MAIN-03/TEST-01 remain open for their uncompleted work. This authorization does
not assert hardware validation or authorize pushing. Existing test evidence is
retained; reconstruction is labelled and commit dates are not backdated.

## MAIN-03 W06 / TEST-01 W06 — qualified; acceptance pending

Implemented the C++ startup helper and verified !boot.obey -> EXEC /autoexec.txt
on raw images. Ten scenario checks passed, alongside 13 SHA boundary vectors,
seven accepted/seven rejected script forms and nine continuation controls.
W06 awaits Author acceptance/checkpoint; W07 remains frozen and unstarted.

1. Added target script scanning/selection persistence, complete hash-linked run
   artifacts, namespace/counter allocation before execution, same-boot CRC-protected
   continuation and exclusive run directories. Shared capture/recording/report
   components supply execution and evidence; no separate report engine.
2. Function failures persist and later groups execute; infrastructure/script-change
   errors stop EXEC. Full/single/commented selections, missing group/finalize,
   unknown functions and repeat-boot preservation passed. Current default has two
   passes plus an unsupported UART case, correctly reported as limited coverage.
3. Added human bundle/startup/startup-check commands and hardware copy guidance.
   Emulator observer verifies runtime identities, retrieves card/SRAM evidence,
   and closes all owned processes. Host durations are recorded; -u is not timing
   evidence. Positive/negative human CLI headlines and statuses passed.
4. First trial stopped before tests because duplicate exclusive artifact copies
   collided; fixed and retained trial. Final qualification-03 includes continuation
   integrity checks. A supplemental zero-fill assumption was disproved by FF SRAM
   bytes; no system writer or fresh guard test was inferred. Evidence is archived
   under MAIN-03/W06 to avoid proliferating loose artifact files.
5. Hardware, power-loss atomicity, arbitrary crash recovery and real MOS API cases
   remain unqualified. MOS-01 stays parked; no firmware changes, commit or push.
   On acceptance, commit W06 results before proceeding to the frozen W07 contract.

## W06 accepted; W07 authorized

The Author said “proceed” after the explicit proposal to accept/commit W06 and
then execute W07. Recording W06 acceptance and committing its implementation,
evidence and disposition with the unchanged frozen W07 contract before beginning
aggregate qualification. This does not authorize physical hardware work or a push.

## MAIN-03 W07 — aggregate qualification passed

After committing accepted W06 as 6bc13e9, ran all foundation controls, smoke
regression and an independent fresh raw-image selection. All 12 command stages
passed (119.309 seconds total host wall time). Ten prior W06 result files were
unchanged, repeated boots preserved earlier runs, and no Fab processes remained.
Controlled pre-reset SRAM capture and decoder use passed; arbitrary crash
recovery, hardware and power loss remain unqualified. Added human qualification
guidance reused by agents, and retained the complete evidence archive and source
identities under docs/tasks/MAIN-03/W07. W07 awaits acceptance/checkpoint; no push.
No production behavior changed and MOS-01 stays parked.

## Restart recovery design and sequencing

The Author requested the restart recovery design and TODO placement. Added the
planned gate to docs/test-strategy.md and created MAIN-05 with immutable W01–W05.
It follows MAIN-03/TEST-01 acceptance and precedes the proposed MAIN-04 MOS slice;
MAIN-04 keeps its proposed identity. Recovery runs before allocation/execution,
uses SD checkpoints plus a validated locator journal, preserves original evidence
and stops repeated boots until explicit disposition. Missing/damaged state and
interrupted acknowledgement fail conservatively; SRAM is not reset-persistent.
No cause is inferred from interruption alone and no power-loss guarantee is made.
This change is documentation/planning only; W07 acceptance remains pending and
no implementation, commit or push was performed.

## W07 accepted; MAIN-05 W01 checkpoint

The Author said “go for it” to accepting/committing W07 and freezing the recovery
contract. Marked MAIN-03 and TEST-01 accepted/closed, removed their unfinished
TODO entries, and froze MAIN-05 W01 scope, deliverables and validation before
contract design. Commit includes the accepted W07 archive and prior approved
recovery design/task sequencing. No push and no hardware claim.

## MAIN-05 W01 contract completed

After checkpoint ecec9b9, specified the 256-byte dual-file journal, publication
ordering, startup reconciliation limits, conservative reset-window decisions and
explicit park/re-arm semantics. Reviewed current allocator, lifecycle and close
hooks. Retained 30 independently stated transitions and an IDLE-slot wire fixture.
W01 awaits acceptance/checkpoint; no W02 code, emulator run or push. Hardware and
power-loss durability remain unqualified. Corrected one W07 script EOF whitespace
warning without changing behavior.

## W01 accepted; W02 authorized

The Author requested “push, then do your next work.” This accepts W01 for its
result checkpoint, freezes W02 implementation/validation scope, and authorizes
pushing these commits before implementing the gate. W03 dispositions remain
separate. No hardware execution or firmware repair is authorized.

## MAIN-05 W02 — startup recovery gate qualified

Accepted/checkpointed W01 as 8fa4b5a and verified the push to origin. Implemented
the C++ two-slot journal, ordered allocation/case/finalization publications,
bounded evidence reconciliation, same-boot session checks and read-only inspect.
Human/agent tooling retrieves the target decision and prevents file-only all-pass
when journal/command evidence is unresolved. Promoted W01 fixture/oracles.

Final check-04 passed 17 raw-image boots (189.629 seconds observed boot/retrieval
time), 2,048 native bit corruptions, 121 transition decisions, malformed record
checks and explicit report uncertainty. Interrupted/repeated boots preserve all
prior evidence and counters, actual case-entry interruption is identified, and
completed failures remain failures while permitting the next boot. Startup/report/
smoke regressions passed; no owned emulator remained. Evidence and limits are in
docs/tasks/MAIN-05/W02.

Encountered and retained an AgonDev i9 legalization compiler crash; equivalent
transition-table source avoids it. No compiler/firmware patch or upstream contact.
W02 remains uncommitted pending acceptance; no W03 implementation. Hardware,
physical power-loss behavior and arbitrary crash recovery remain unqualified.

## W02 accepted; W03 authorized

The Author explicitly accepted W02 and requested commit then next work. This
checkpoint records accepted W02 results and freezes the W03 integration contract
(requests, acceptance certificates and child schema2) before implementation.
No push is requested for this checkpoint. Hardware remains unqualified.

## MAIN-05 W03 — implementation and initial qualification

Committed accepted W02 and the W03 contract as a4694a9 (no push requested).
Implemented bounded target request parsing, DISPOSING snapshots/receipts,
PARKED/ARMED acceptance certificates, consume-before-allocation child authorization,
schema2 child manifests and bound receipt artifacts. Shared human recover helper
prepares requests and executes the same target command through a temporary boot
chain, restoring !boot.obey. Parent verdicts remain separate.

Trial-01 parked successfully but produced malformed evidence-map JSON and the
following boot correctly rejected it. Replaced cross-call first-entry bookkeeping
with writer-buffer delimiter state and added complete generated-receipt parse/
evidence validation before acceptance. This is not a proven compiler defect.
Trial-02 passed park, repeated blocked boot, selected continuation and a passing
one-case child while preserving its incomplete parent. Native qualification first
caught a test-harness temporary-string lifetime error; fixed the harness to own
the parser input. Production target uses persistent scratch storage.
Full qualification and regressions are in progress; do not treat these initial
trials as W03 acceptance. No firmware/compiler patch or upstream contact.

Focused W03 qualification passed 29 boots and 18 check groups. A separate
three-boot deliberate-parent-failure check also passed: child pass counts stay
separate, confirmed parent failure survives and original bytes are unchanged.
Receipt/certificate tampering, stale requests and interrupted disposition/child
allocation all stayed stopped as required. Existing gate/startup/smoke regressions
are running sequentially. All work remains on Linux.

W03 final regressions passed: recovery-check 17 boots plus 2,048 corruption/
121 transition controls; startup-check 10 boots; report-check 58 scenarios,
690 truncations and 690 mutations; smoke C++ and assembly variants. Python syntax,
front-end dispatch and scoped whitespace checks passed. No project-owned emulator
remains. Archived focused/regression evidence and final source hashes under W03;
full disposable card images remain in ignored Linux runtime storage.
W03 is ready for acceptance, uncommitted. W04 has not started; no push, hardware
claim, firmware/compiler patch or upstream contact.

## W03 accepted; W04 authorized

The Author requested commit, push and the next task. W03 results are accepted.
Freeze W04 Q01–Q05 (observational boundaries, independent matrix, interruption/
damaged-state qualification and evidence delivery) with this result checkpoint.
The work remains Linux-only, headless and raw-image based; no hardware or firmware
change is authorized. Preserve unrelated agon-utils edits.
