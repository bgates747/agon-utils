# UI-004 — Extend the font grid zoom through 800%

Status: **Completed under delegated implementation authority; uncommitted.**
Created: 2026-09-10.
Completed: 2026-09-10; removed from the unfinished-work index.

The author requests larger atlas/grid viewing scales, up to 800%, independently
of the accepted application UI-size setting. Implementation and verification
are authorized without a further approval gate; do not commit the changes.

1. [x] Retain existing zoom choices and add 500%, 600%, 700%, and 800% to the
   dropdown and +/− navigation, with safe endpoint behavior.
2. [x] Keep enlarged artwork accessible through scrolling without forcing the
   application window to the size of the entire zoomed atlas.
3. [x] Map clicks and selection/grid overlays correctly at all zooms and scroll
   offsets, including narrow glyphs and non-default atlas row layouts.
4. [x] Verify zoom leaves source glyph pixels and exported font bytes unchanged
   and that repeated zoom/redraw operations do not accumulate canvas items.

UI-003 owns character-editor interaction. Coordinate and redraw changes can be
shared between the tasks; their detailed results belong in the development log.

All added levels, endpoint navigation, fixed viewport requests, scrolled glyph
255 selection at 800%, unchanged `.font` bytes, and 100 repeated zoom/edit cycles
pass integration tests. Narrow 3×3 glyphs at 25% no longer produce a zero grid
step; non-16-column layouts and partial final rows are covered. See the
[development evidence](../development/2026-09-10.md) and
[current behavior](../font-grid-editor.md). No commit was made.
