# BUG-003 — Restore original-source recipes in saved XML

Status: **Completed — source recipes and bitmap opens verified.**
Created and completed: 2026-09-10. Removed from the unfinished-work index.
Behavior: [Source Recipes and Bitmap Export](../document-io.md).
Evidence: [Development log](../development/2026-09-10.md).

The author clarified that XML records the original PNG/TTF source and rendering
settings. BUG-001/BUG-002 incorrectly replaced this recipe with metadata for the
exported bitmap. Repair this now under the ongoing authorization; do not commit.

1. [x] Locate the original Art Deco PNG and document its actual import geometry.
   Found the aligned 1024×2048 sheet with 64×128 cells; regenerated a 6×12 atlas
   with all four quantized gray levels without modifying source assets.
2. [x] Save the original absolute source path and unflattened rendering settings
   in XML. Keep exported bitmap geometry separately so direct bitmap opening
   still preserves saved pixels, including hand edits and quantized shades.
   Source settings remain at XML root; exported geometry is nested in `bitmap`.
   File Save also rejects overwriting the original source with rendered output.
3. [x] Reopen XML and resume after Save using the original source recipe. Loading
   a raw FONT must not reload sidecar rendering choices over live controls.
   Actual Tk startup restores source and recipe; legacy/new FONT controls work.
4. [x] Regress PNG and TTF recipes, monochrome/PNG exports, restart, live raster
   changes, missing sources and legacy metadata headlessly. Update current
   specifications and the dated log; preserve the frozen audit and user assets.
   All 18 editor checks passed under Xvfb; source/render probes also run without
   DISPLAY/WAYLAND_DISPLAY. Earlier round-trip tests now distinguish XML recipes
   from directly opened bitmap output, as explicitly requested by the author.

Recovered source:
`/home/smith/Agon/mystuff/AgonJukebox/src/fonts/art-deco-concept-01/aligned-01/ArtDeco_Concept_64x128.png`.
Its existing XML and README describe 16 columns, ASCII 0–255, and zero offsets.
The source is 1-bit, but bicubic downscaling followed by quantization produces
four gray levels. For 6×12 cells, use `scale_width=-58`, `scale_height=-116` and
`raster_type=quantized`. These are reconstructed size settings, not recovered
proof of the author's exact previous recipe. Existing exports are unchanged.

Scope boundary: pitch/gutter-aware PNG import remains unimplemented. The current
reader divides the complete sheet by the configured character grid. A portable
project bundle containing source artwork, rendering settings and edited glyphs,
with explicit Agon export, is a design proposal for later discussion. It is not
part of this repair. XML recipes regenerate artwork; exported pixels remain the
record of hand edits until a proper editable document format exists.
